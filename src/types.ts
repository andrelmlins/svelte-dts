export type Options = {
  output: string;
};

export type Prop = {
  name: string;
  type: string;
  isOptional: boolean;
};

export type Event = {
  name: string;
  type: string;
};

export type SlotProp = {
  name: string;
  type: string;
};
